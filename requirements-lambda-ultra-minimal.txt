# Ultra minimal Lambda dependencies for SST deployment
# Only essential packages without any numpy/scipy dependencies

# Core MongoDB (no langchain for now to avoid numpy)
pymongo==4.6.1

# AWS Lambda
mangum==0.17.0

# FastAPI for Lambda
fastapi==0.104.1

# Utilities
python-dotenv==1.0.0
pydantic>=2.7.4
pydantic-settings>=2.5.2
boto3>=1.35.0

# OpenAI (minimal)
openai>=1.86.0,<2.0.0
requests==2.31.0