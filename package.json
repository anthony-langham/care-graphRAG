{
  "name": "care-graphRAG",
  "version": "0.1.0",
  "description": "GraphRAG over NICE CKS (Hypertension) on Cloudflare Workers + D1",
  "private": true,
  "scripts": {
    "deploy": "npm --prefix worker run deploy",
    "dev": "npm --prefix worker run dev",
    "tail": "npm --prefix worker exec wrangler tail"
  }
}
